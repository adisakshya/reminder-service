apiVersion: v1
kind: Service
metadata:
  name: dev-reminder-service
  namespace: development
  labels:
    app: dev-reminder-service
spec:
  type: NodePort
  selector:
    app: dev-reminder-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
