apiVersion: v1
kind: Service
metadata:
  name: prod-reminder-service
  namespace: production
  labels:
    app: prod-reminder-service
spec:
  type: NodePort
  selector:
    app: prod-reminder-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
